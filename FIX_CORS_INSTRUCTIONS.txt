========================================
CORS HATASINI DÜZELTME TALİMATLARI
========================================

🔴 SORUN:
- Access to XMLHttpRequest has been blocked by CORS policy
- 500 Internal Server Error

✅ ÇÖZÜM:

1️⃣ Backend klasörüne gidin:
   C:\Users\Slayerx7t\Desktop\ticaret-sitesi\ticaret

2️⃣ config/cors.php dosyasını açın ve şu şekilde değiştirin:

<?php

return [
    'paths' => ['api/*', 'sanctum/csrf-cookie'],
    'allowed_methods' => ['*'],
    'allowed_origins' => [
        'http://localhost:5173',
        'http://127.0.0.1:5173',
        'http://localhost:5174',
    ],
    'allowed_origins_patterns' => [],
    'allowed_headers' => ['*'],
    'exposed_headers' => [],
    'max_age' => 0,
    'supports_credentials' => false,
];

3️⃣ .env dosyasını açın ve şu satırları ekleyin/güncelleyin:

APP_URL=http://127.0.0.1:8000
FRONTEND_URL=http://localhost:5173
SESSION_DRIVER=cookie

4️⃣ app/Http/Kernel.php dosyasını açın:

protected $middleware = [
    // Diğer middleware'ler...
    \Fruitcake\Cors\HandleCors::class,  // ← BU SATIRI EKLEYIN (en üste)
];

protected $middlewareGroups = [
    'api' => [
        \Fruitcake\Cors\HandleCors::class,  // ← BU SATIRI EKLEYIN (en üste)
        'throttle:api',
        \Illuminate\Routing\Middleware\SubstituteBindings::class,
    ],
];

5️⃣ Terminal'de şu komutları çalıştırın:

cd C:\Users\Slayerx7t\Desktop\ticaret-sitesi\ticaret
php artisan config:clear
php artisan cache:clear
php artisan route:clear

6️⃣ Sunucuyu yeniden başlatın (zaten çalışıyor ama yeniden):

Ctrl+C ile durdurun
php artisan serve --host=127.0.0.1 --port=8000

========================================
ŞU AN ÇALIŞAN SUNUCULAR:
========================================

✅ Backend:  http://127.0.0.1:8000
✅ Frontend: http://localhost:5173

========================================
TEST İÇİN:
========================================

http://localhost:5173 adresine gidin
Admin paneline giriş yapmayı deneyin
Console'da hata olup olmadığını kontrol edin

========================================
